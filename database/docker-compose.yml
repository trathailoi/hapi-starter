version: '3'

services:
  postgres_loi_exercise:
    image: postgres:9.6
    container_name: postgres_loi_exercise
    environment:
      POSTGRES_USER: sevenmiles
      POSTGRES_PASSWORD: Passw0rd!
      # POSTGRES_DB: postgres
    # volumes:
    #   - ./data/postgres:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    restart: always